import makeStyles from "@mui/styles/makeStyles";

const useStyles = makeStyles({
  home: {
    marginLeft: "72px",
    marginTop: "30px",
    paddingLeft: "50px",
    width: "calc(100% - 72px)",

    minHeight: "100vh",
  },
});

export default useStyles;
