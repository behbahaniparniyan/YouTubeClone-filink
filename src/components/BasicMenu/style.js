import makeStyles from "@mui/styles/makeStyles";

const useStyles = makeStyles({
  moreIconColor: {
    color: "#FFF",
  },

  moreIconLightColor: {
    color: "#7E7E7E",
  },
});

export default useStyles;
